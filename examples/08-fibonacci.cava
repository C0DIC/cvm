// Fibonacci numbers to 10946

main:
    push 10946   // Limiting iterations

    push 0       // First number
    wrtn i64

    push 1       // Second number
    wrtn i64

    loop:
        dup 1   // Duplicate
        dup 1   // Duplicate


        add       // Sum
        wrtn i64  // Print

        cmp 0     // Compare last number with limit
            hlt

    goto loop     // Repeat