// Fibonacci numbers to 10946

main:
    push 10946   // Pushing limit number 
    push 0       // Fisrt number
    push 1       // Second number

    loop:
        dup  1       // Duplicate
        dup  1       // Duplicate

        add          // Sum it up
        wrt          // Print

        cmp 0        // Compare last number with limit (first line of main)
            hlt

    goto loop       // Repeat