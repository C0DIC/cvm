push 15   // Pushing 15 to the stack          <- Instruction #0
push 15   // Pushing 15 to the stack          <- Instruction #1

goto exit    // Skiping wrt instruction (instructions count starts with 0)  <- Instruction #2
wrtn i64      // <- Instruction #3

exit:
    hlt       // <- Instruction #4