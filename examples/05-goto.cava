push 15   // Pushing 15 to the stack          <- Instruction #0
push 15   // Pushing 15 to the stack          <- Instruction #1

goto 4    // Skiping wrt instruction (instructions count starts with 0)  <- Instruction #2
wrt       // <- Instruction #3

hlt       // <- Instruction #4